// Prisma is only available when DATABASE_URL is configured.
// The app works fully without a database (all data is local).
// To use: set DATABASE_URL in .env and run `npx prisma generate`

export const prisma = null;
